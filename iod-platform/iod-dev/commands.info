
----------------------------------------------------
Coverage commands

pytest --cov=apps --cov-report=html -n 10

--> coverage run -m pytest
--> coverage report
--> coverage html

cd ~/iod-web/htmlcov
python3 -m http.server 8000

demo url : 
http://127.0.0.1:8000/app/demo

pytest

pytest -s : gives you output of test runs
pytest --collect-only 

pytest debug commands

pytest tests/configuration/test_views.py::Test_AssetType_Views --html=report.html --self-contained-html

pytest tests --html=report.html --self-contained-html

# Parallel execution
--> pytest -n auto
--> coverage run -m pytest -n auto

### Make necessary changes in future for parallel execution 
coverage run --parallel-mode -m pytest -n auto --cov-append

python ..\manage.py startapp esite

--------------------------------------------------------------------------------------------------------------------------------
Management commands
--------------------------------------------------------------------------------------------------------------------------------
python manage.py migrate_usertour
python manage.py delete_user joebloggs@email.com

--------------------------------------------------------------------------------------------------------------------------------
LOCAL SMTP
--------------------------------------------------------------------------------------------------------------------------------
python manage.py run_local_smtp

--------------------------------------------------------------------------------------------------------------------------------
Logging
--------------------------------------------------------------------------------------------------------------------------------

The possible log levels are DEBUG, INFO, WARNING, ERROR, and CRITICAL. 

--------------------------------------------------------------------------------------------------------------------------------
############# Database backup

pg_dump -U myuser -h localhost -p 5432 -d mydatabase -f /path/to/backup.sql
pg_dump -U postgres -h localhost -p 5432 -d sdiarydos -f F:\src\sdiary-bckup\28052023\backup.sql

########## suggested names for  tracker total
trail
robin

##### DATABASE RESTORE

psql -U <username> -h <hostname> -p <port> -d <database_name> -f <input_file>

psql -U postgres -h localhost -p 5432 -d restoremydb -f F:\src\sdiary-bckup\28052023\backup.sql

#sample test



----------------------------------------------------------------------------------------------------------------------------------------------
UI django pro 
----------------------------------------------------------------------------------------------------------------------------------------------

1. demo index page is at below url

http://127.0.0.1:8000/app/demo/pages/index

http://127.0.0.1:8000/app/demo/elements-icon-feather.html

2. If you want to enable any demo page, go to demo/includes/sidebar.html

example 1 :

--> change url for anchor tag in sidebar.html [so that links are routed under pages]
<li><a href="dashboard-ecommerce.html">Ecommerce</a></li>
from
<li><a href="/dashboard-ecommerce.html">Ecommerce</a></li>

let take an example if you want to enable Dashboard - dashboard-ecommerce

--> navigate to dashboard-ecommerce.html page in demo/home/
--> change layout reference 
    {% extends "demo/layouts/base.html" %}
    instead of
    {% extends "layouts/base.html" %}

